{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cp .env.example .env && php artisan key:generate --force && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
